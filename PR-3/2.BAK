#include<stdio.h>
#include<conio.h>
main()
 {
	    int n,length = 0 ;
   clrscr();
	   printf("Enter the velue");
	   scanf("%d",n);
	 while(n!=0)
	 {
	  n=n/10;
		length++;
	 }

	 printf("%d",length);
  getch();

 }