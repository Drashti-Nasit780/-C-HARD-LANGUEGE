#include<stdio.h>

main()
{
	int size ;
	 printf("Enter of the size : ");
	 scanf("%d",&size);
	 int a[size],b[size];
	 for(i=1;i<=size;i++)
	 {
	  printf("Enter a[%d]",a[%d]);	
	  scanf("%d",&a[i],);
	 }
	 for(i=1;i<=size;i++)
	 {
	  printf("Enter b[%d]",b[i]);	
	  scanf("%d",b[i]);
	 }
	int sum;
	   
	  sum a[i]b[i]+=a[i]+b[i];
	  
	  printf("the value of sum",sum);
	 
}
