#include<stdio.h>

main()

{
	int a=100;
	while(a>10)
	{
		printf(" number of the series",a);
		a--;
	}
	    printf("the number of series",a);
    if(a>=1 || a!=0)
    {
    	printf("your make series",a);
    	scanf("%d",&a);
	}
}
