#include<stdio.h>
#include<conio.h>
main()
{
	   int a,b,c,d,max;
   clrscr();
	  printf("Enter the number ");
	  scanf("%d",&a);
	  printf("Enter the number ");
	  scanf("%d",&b);
	  printf("Enter the number");
	  scanf("%d",&c);
	if(a<b)
	  {
	      printf("Enter the max number");
	  }
	else if(b<c)
	{
	      printf("Enter the max number");
	}
	else if(c<b)
	       {
		printf("Enter the max number");
	       }
	 else(b<c)
		{
		 printf("Enter the mixmum velue");
		}
	    printf("enter the number;",a,b,c,d);

   getch();

}